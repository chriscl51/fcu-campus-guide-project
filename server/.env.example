# Copy this file to .env and fill in a real value. .env is gitignored — the
# real password never gets committed. See index.js for how this gates the
# write endpoints (POST/PUT/DELETE /api/events) and AdminView.vue's login
# gate on the client side.
ADMIN_PASSWORD=change-me
